import LayoutAdmin from '@/layouts/admin'
import { LayoutProps } from '@/models/layout'
import React from 'react'

type Props = {}

const Admin = (props: LayoutProps) => {
  return (
    <div>Admin</div>
  )
}

Admin.Layout = LayoutAdmin

export default Admin