import Notfound from '@/layouts/Notfound'
import { LayoutProps } from '@/models/layout'
import React from 'react'

type Props = {}

const NotFound = (props: LayoutProps) => {
  return (
    <div>NotFound</div>
  )
}

NotFound.Layout = Notfound

export default NotFound